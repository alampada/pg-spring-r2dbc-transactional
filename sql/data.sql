CREATE TABLE a(
	id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	name varchar unique
);

CREATE TABLE b(
	id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	name varchar
);

INSERT INTO a VALUES(1, 'clash');